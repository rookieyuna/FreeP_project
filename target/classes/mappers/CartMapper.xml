<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace ="cart.CartImpl">
  
  	<select id="listPage" resultType="cart.CartDTO">
  	
  		select ct_idx, ct_name, ct_count, p_sfile, p_size, p_price
			from cart C, product P
			where 
    			C.ct_code = P.p_code
   				and C.m_code = #{param1}
  	</select>
  	
  	<select id="listPagediy" resultType="cart.CartDTO">
  	
  		select ct_idx, ct_name, ct_count, d_price, dough, sauce, topping1, topping2, topping3, topping4, topping5
			from cart C, diy d
			where 
    			C.ct_code = d.diy_idx
   				and C.m_code = #{param1}
  	</select>
  	
 
  	<select id="objectdiy" resultType="cart.CartDTO">
  	
  		select p_name, p_price, p_size 
		from product
		where p_code = #{param1}
  	</select>

	<select id="sum1" resultType="int">
	select sum(p_price)
			from cart C, product P 
			where 
    			C.ct_code = P.p_code
   				and C.m_code = #{param1}
	</select>
	<select id="sum2" resultType="int">
	select  sum(d_price)
			from cart C, diy d
			where 
    			C.ct_code = d.diy_idx
   				and C.m_code = #{param1}
	</select>
	
	<delete id="deleteCart">
		delete from cart where ct_idx=#{param1}
	</delete>
  </mapper>